replicaCount: 1
deploymentName: mirror
namespace: mirror
image:
  repository: wlanboy/mirrorservice
  tag: latest
  pullPolicy: IfNotPresent

hosts: 
  - mirror.tp.lan
  - mirror.gmk.lan

service:
  port: 8080

persistence:
  enabled: true
  size: 100Mi
  storageClass: standard

istio:
  gateway: mirror-gateway
  gatewayNamespace: "istio-ingress"

certmanager:
  issuer: local-ca-issuer
  kind: ClusterIssuer
  secretName: mirrorapi-tls
